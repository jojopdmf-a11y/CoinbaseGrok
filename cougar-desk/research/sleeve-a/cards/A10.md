# RULESET | A10 | 2026-09-10

- family: IBIT-lead laggard
- coins: ETH SOL HYPE (BTC is the **signal**, not the trade)
- products: trade ETHA, BSOL, BHYP. IBIT is signal-only. Do not buy IBIT on this card.
- already burned cousins: not A6 (A6 is own-name 10-day return in the top quartile). Not A4 (own 20-day high).

## Entry (exact bars)

On session `t` (all closes known):

1. IBIT `Close[t]` is a **10-day high**: `Close_IBIT[t] ≥ max(High_IBIT[t-9..t])` using IBIT highs, or equivalently `Close_IBIT[t] ≥ max(Close_IBIT[t-9..t])` — pick **close vs prior 9 closes** and write which you used in the result. Default: `Close_IBIT[t] ≥ max(Close_IBIT[t-9] … Close_IBIT[t])`.
2. For each trade name `X` in {ETHA, BSOL, BHYP}:
   - `Close_X[t] < max(Close_X[t-9] … Close_X[t-1])` (not making its own 10-day close high)
   - `Close_X[t] < SMA20_X[t]`
   - `SMA50_X[t] ≥ SMA50_X[t-5]` (flat or up)
3. Enter `Open_X[t+1]`. If two names qualify the same day, take **ETHA then BSOL then BHYP** (one fill). Skip if a Sleeve A slot is already full in the replay (max two names).

## Exits

- Stop 2.5%. Target 7.5%. Time stop 10 trading days.
- Same bar: stop first.
- Fees: 5 and 10 bps RT. BHYP also 15–25. If BHYP ADV cannot hold ~$4k, cut notional or skip HYPE and say so.

## Sentiment

None on the first pass.

## Data

Aligned daily sessions. If BHYP is missing a day IBIT traded, skip that name that day.

## Result

- Per product: n / WR / E @10bps / max DD
- WF1 E / rolling OOS mean E
- verdict: PASS | FAIL | n-fail
- next:
