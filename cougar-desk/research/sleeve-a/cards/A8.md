# RULESET | A8 | 2026-09-10

- family: post-print digest
- coins: BTC ETH SOL (HYPE only if BHYP has ≥ 20 event-adjacent bars; else skip HYPE)
- products: IBIT ETHA BSOL (BHYP optional)
- already burned cousins: not “wait for CPI.” Not A7 vol expansion. Not A4 breakout. Catalysts do not pause the factory; this ID *is* the event rule.

## Calendar (required before any backtest)

Write `cougar-desk/research/sleeve-a/calendars/us-macro.md` with every date used.

- Events: US CPI (all-items, 8:30 ET), FOMC policy decision (typically 14:00 ET), NFP (8:30 ET)
- Columns: `date`, `event`, `release_et`, `source_url`
- Window: first IBIT bar through last complete regular session
- No PASS / FAIL without that file checked in. Do not invent dates from memory.

## Entry (exact bars)

Define `print_session` as the ETF regular session whose **date** is the US calendar date of the release (even if the print is 8:30 ET, before the open).

1. `D` = `print_session` for a row in the calendar.
2. Ignore D if the ETF did not trade that day; use the next session as D.
3. Signal (all must hold on D’s **close**):
   - `Close[D] > High[D-1]`
   - `Close[D] > SMA50[D]`
4. Enter `Open[D+1]`. One open position per product. If already in a trade, skip.

## Exits

- Stop 2.5% of entry. Target 7.5%. Time stop **5** trading days (shorter than the burned 10-day sheet).
- Same bar: stop first.
- Fees: 5 and 10 bps RT. HYPE also 15–25 if you run BHYP.

## Sentiment

None on the first pass. `A8s` (yesterday `sent_score` ≥ 0) only after A8 has a full table.

## Data

Yahoo or Schwab daily OHLC. SMA50 warmup 50 bars.

## Result

- n / WR / E @10bps / max DD / trades per year
- WF1 E / rolling OOS mean E
- verdict: PASS | FAIL | n-fail
- next:
