# RULESET | A11 | 2026-09-10

- family: NR7 then close-through
- coins: BTC ETH SOL HYPE
- products: IBIT ETHA BSOL BHYP
- already burned cousins: not A4 (20-day high + SMA50). Not ruleset #2 (1-day poke, n-gate fail). Not A7 (ATR vs its median).

## Entry (exact bars)

Range on session `t`: `Range[t] = High[t] − Low[t]`.

1. NR7: `Range[t] = min(Range[t-6] … Range[t])` and `Range[t] > 0`.
2. `SMA50[t] ≥ SMA50[t-5]`.
3. Wait one session. Signal on `t+1` close: `Close[t+1] > High[t]` (close through the NR7 high, not a wick-only poke).
4. Enter `Open[t+2]`. One open per product.

Do **not** enter at a stop through `High[t]` on t+1. Daily paper cannot fill that without lookahead. Close-through then next open only.

## Exits

- Stop 2.5%. Target 7.5%. Time stop 10 trading days.
- Same bar: stop first.
- Fees: 5 and 10 bps RT. BHYP also 15–25.

## Sentiment

None on the first pass.

## Data

Yahoo or Schwab daily. SMA50 warmup 50 bars. Need 7 bars of range before the first NR7.

## Result

- n / WR / E @10bps / max DD / trades per year
- WF1 E / rolling OOS mean E
- verdict: PASS | FAIL | n-fail
- next:
